const slider=document.querySelectorAll(".slider"),btnPrev=document.getElementById("prev-button"),btnNext=document.getElementById("next-button");let currentSlide=0;function hideSlider(){slider.forEach(e=>e.classList.remove("on"))}function showSlider(){slider[currentSlide].classList.add("on")}function nextSlider(){hideSlider(),currentSlide===slider.length-1?currentSlide=0:currentSlide++,showSlider()}function prevSlider(){hideSlider(),0===currentSlide?currentSlide=slider.length-1:currentSlide--,showSlider()}btnNext.addEventListener("click",nextSlider),btnPrev.addEventListener("click",prevSlider),setInterval(nextSlider,5e3);